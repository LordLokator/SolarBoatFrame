python-can # CAN bus module
python-dotenv # For reading the .env config

shapely # For checking if a polygon contains a point @ GPS
pyproj  # Python interface to PROJ (cartographic projections and coordinate transformations library)

loguru # Logger